# 跌倒检测训练数据生成 - 多进程并行化优化报告

## 1. 背景与问题

### 问题分析
- Windows 上 cv2 (OpenCV) 不支持 fork 模式的多进程，会导致 DLL 加载失败
- 单进程内 `--workers N` 参数在 Windows 上不稳定
- 主要瓶颈在 **Pose 检测（GPU）**，特征计算本身很快（毫秒级）

### 优化方案
使用多个独立 Python 进程代替单个进程内的多线程，通过 `--offset` 和 `--max-videos` 参数处理不同的视频批次。

## 2. 测试结果（完整流程，含 Pose 检测）

### 测试环境
- CPU: 16 核心
- 测试视频: 4 个已有 pose 结果的视频
- 测试模式: 完整流程（切帧 + Pose 检测 + 特征计算）

### 各视频详细耗时

| 视频 | 单进程 | 2进程 | 4进程 |
|------|--------|-------|-------|
| 0407add_IR_positive_0004 | 42.3s | 53.1s | 60.1s |
| 0407add_IR_positive_0006 | 22.1s | 34.2s | 35.7s |

### 汇总对比

| 配置 | 总耗时 | 加速比 | 备注 |
|------|--------|--------|------|
| 单进程 (baseline) | 65.1秒 | 1.00x | 顺序执行 |
| 2进程并行 | 55.0秒 | 1.19x | 每进程处理2个视频 |
| 4进程并行 | 62.6秒 | 1.04x | 每进程处理1个视频 |

## 3. 测试结果（仅特征计算，跳过 Pose 检测）

### 测试环境
- 测试模式: `--reuse-frames`（跳过切帧和 Pose 检测）

### 耗时对比

| 配置 | 总耗时 | 加速比 | 备注 |
|------|--------|--------|------|
| 单进程 (baseline) | 197.3秒 | 1.00x | 顺序处理 |
| 2进程并行 | 77.0秒 | 2.56x | 每进程处理2个视频 |
| 4进程并行 | 68.2秒 | 2.89x | 每进程处理1个视频 |

## 4. 关键发现

### 发现1：完整流程下多进程效果有限
- 加速比最高只有 **1.19x**（2进程）
- 4进程反而比2进程更慢（62.6s vs 55.0s）

### 发现2：仅特征计算时多进程效果显著
- 加速比达到 **2.89x**（4进程）
- 因为瓶颈不在 GPU 推理，而在 CPU 计算

### 发现3：瓶颈分析
```
完整流程瓶颈：
├── 切帧: I/O密集（cv2读取视频）
├── Pose检测: GPU密集（YOLO推理）← 主要瓶颈
└── 特征计算: CPU密集（已并行化）

仅特征计算瓶颈：
└── 特征计算: CPU密集 ← 多进程有效
```

### 发现4：进程数增加反而变慢的原因
1. **YOLO 模型加载开销**：每个进程都要加载模型（~2-3秒）
2. **GPU 资源竞争**：多进程竞争同一 GPU
3. **进程调度开销**：进程切换成本

## 5. 优化建议

### 场景1：完整流程（切帧 + Pose 检测 + 特征计算）
- **推荐：单进程** 或 **2进程**
- 原因：瓶颈在 GPU，多进程会竞争资源

### 场景2：仅特征计算（跳过切帧和 Pose 检测）
- **推荐：4进程**
- 原因：瓶颈在 CPU，多进程有效

### 场景3：大规模批量处理
```bash
# 分批处理，每批4个视频，使用4个独立终端
# 终端1
python main_pipeline.py --batch --max-videos 4 --offset 0 --workers 1
# 终端2
python main_pipeline.py --batch --max-videos 4 --offset 4 --workers 1
# ...以此类推
```

## 6. 新增代码

### --offset 参数（main_pipeline.py）
```python
parser.add_argument("--offset", type=int, default=0, help="视频列表起始偏移量")
```

### run_parallel.py
自动检测 CPU 核心数，分配视频批次，启动多进程。

## 7. 结论

1. **多进程效果取决于瓶颈位置**：
   - GPU 瓶颈（完整流程）：多进程无效，反而更慢
   - CPU 瓶颈（仅特征计算）：多进程有效，2.89x 加速

2. **推荐配置**：
   - 完整流程：使用 2 进程或单进程
   - 仅特征计算：使用 4 进程

3. **进一步优化方向**：
   - 多 GPU 并行（当前是单 GPU）
   - 共享 YOLO 模型（减少加载开销）

---
生成时间: 2026-05-14
测试数据: 4 个视频（两种模式对比）